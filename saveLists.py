import cpgcsv
import main
import pickle

# Assuming the list is already populated and stored in the 'lists' variable

# Save the list to a file
import pickle

# Dictionary to store


# Store the dictionary using pickle
with open('my_dict.pkl', 'wb') as file:
    pickle.dump(cpgcsv.result_dict, file)

